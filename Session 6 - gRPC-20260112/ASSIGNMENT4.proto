syntax = "proto3";

package assignment4;

// The request message containing distribution type and the parameter
message GenerateRequest {
    string distr_type = 1; // "deterministic", "uniform", or "exponential"
    double parameter = 2;   // fixed time, T, or lambda
}

// The response message returning the time spent and a status message
message GenerateResponse {
    double generated_time = 1;
    string message = 2;
}

// Service definition
service DistributionService {
    rpc GenerateResponseTime(GenerateRequest) returns (GenerateResponse);
}

// TEST

//three distribution test:
// deterministic: python ASSIGNMENT4_CLIENT_done.py -d d -p 2.0 -s localhost -v
// uniform: python3 ASSIGNMENT4_CLIENT_done.py -d u -p 5.0 -s localhost -v
// exponential: python3 ASSIGNMENT4_CLIENT_done.py -d e -p 0.5 -s localhost -v

//multithreading:
//python3 ASSIGNMENT4_CLIENT_done.py -d d -p 3.0 -s localhost -t 5 -v

// to see also if the system is robust to errors I could try to put a bad port or a -p very little like 0.00001
